# 风信子（Hyacinth）打包指南

本文档说明如何把风信子打包成 Windows exe 安装包，以及如何更新版本重新出包。

- 打包链路：**PyInstaller**（onedir 主程序）+ **Inno Setup**（安装向导）
- 版本号唯一来源：`pyproject.toml` 的 `[project].version`
- 一键脚本：`installer\build_installer.ps1`

---

## 一、更新版本出包（日常最常用）

只需三步：

1. **改版本号**：打开仓库根目录的 `pyproject.toml`，把 `[project]` 下的
   `version = "0.1.0"` 改成新版本号（如 `"0.2.0"`）。这是唯一需要手动改的地方，
   exe 属性版本、安装包文件名都会自动跟随。

2. **一键出包**：在仓库根目录执行

   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File installer\build_installer.ps1
   ```

   脚本会依次完成：
   - 从 `pyproject.toml` 读取版本号；
   - 用 PyInstaller 重新构建 `dist\Hyacinth\`（约 40 秒）；
   - 用 Inno Setup 编译安装包（约 30 秒）。

3. **拿包**：产物在

   ```
   installer\Output\Hyacinth-Setup-<版本号>.exe
   ```

   例如 `Hyacinth-Setup-0.2.0.exe`。

## 二、安装包说明（给使用者）

| 能力 | 说明 |
|---|---|
| 中文向导 | 安装界面为简体中文 |
| 选择安装目录 | 安装时可点"浏览"换任意目录；开头可选"仅为我安装（免管理员）/ 为所有用户安装" |
| 快捷方式 | 开始菜单组 + 可勾选的桌面快捷方式；安装完成可直接勾选启动 |
| 升级 | AppId 固定，双击新版本安装包即识别为同一应用，可沿用原目录覆盖升级 |
| 卸载 | 控制面板/开始菜单卸载，只删程序本体，**绝不触碰** `文档\Hyacinth` 用户文件库 |

## 三、前置条件（构建机）

1. **Inno Setup 6**（编译安装包）：从 <https://jrsoftware.org/isdl.php> 下载安装。
   本机已安装（当前用户级：`%LOCALAPPDATA%\Programs\Inno Setup 6`）。
   中文语言包 `installer\ChineseSimplified.isl` 已随仓库自带，换机器无需另找。
2. **uv 环境**（构建 PyInstaller）：仓库根目录 `uv sync` 即可，`pyinstaller`
   已在 dev 依赖中。
3. Windows 10/11 64 位。

## 四、打包链路与关键适配（维护者须知）

### 4.1 文件清单

| 文件 | 作用 |
|---|---|
| `installer/build_installer.ps1` | 一键出包脚本（读版本 → PyInstaller → Inno Setup） |
| `installer/hyacinth.spec` | PyInstaller 配置：onedir、无控制台、收集 `hyacinth/assets` 图标、exe 图标、自动生成版本资源 |
| `installer/hyacinth.iss` | Inno Setup 安装脚本：向导、目录、快捷方式、卸载 |
| `installer/ChineseSimplified.isl` | 官方简体中文语言包（源自 jrsoftware/issrc，6.5.0+） |
| `installer/exe-version-info.txt` | **自动生成**（构建时由 spec 从 pyproject 版本号写出），不要手工编辑、不要提交 |

### 4.2 两处必须的子进程适配（已实现，勿回退）

打包成单 exe 后，`sys.executable` 指向 exe 自身，原生的子进程启动方式会失效：

1. **后台任务队列**（`tasks/manager.py`）用 `multiprocessing spawn` 启动常驻
   Worker → 冻结后子进程会带着 `--multiprocessing-fork` 重新进入 exe，
   由 `__main__.py` 开头的 `multiprocessing.freeze_support()` 接管。
   **该调用必须保持在任何 Qt 初始化之前。**
2. **Excel COM 转换**（`excel/com_engine.py`）原用 `sys.executable -m
   hyacinth.excel.com_worker` → 冻结后改为 `[sys.executable,
   --hyacinth-com-worker, ...]`，由 `__main__.py` 的参数分支转投
   `com_worker.main`。开关常量 `COM_WORKER_FLAG` 定义在 `com_worker.py`，
   入口适配测试见 `tests/test_main_entry.py`。

### 4.3 为什么是 onedir 不是单文件（onefile）

- 常驻 Worker 崩溃/超时后会被重建，onefile 每次重入都要解压整个 PySide6
  运行时（每次数秒），onedir 无此开销；
- onedir 启动明显更快，杀毒软件误报率也更低。

### 4.4 版本同步关系

```
pyproject.toml [project].version   ←—— 唯一手改处
        ├── hyacinth.spec 构建时读取 → 生成 exe-version-info.txt → exe 属性版本
        └── build_installer.ps1 读取 → ISCC /DMyAppVersion=… → 安装包文件名与"添加或删除程序"中的版本
```

## 五、注意事项

- `dist\`、`build\`、`installer\Output\`、`installer\exe-version-info.txt`
  均为构建产物，已在 `.gitignore` 中，不要提交；
- 产品代码改动后无需任何额外操作，直接重跑一键脚本即包含最新代码；
- 正式发布前建议在一台**干净机器**（未装过 Python/uv）上完整装一次验证：
  自定义目录安装 → 快捷方式启动 → 导入 xls（验证 COM 增强引擎）→ 卸载干净；
- Inno Setup 大版本升级（如 6 → 7）后需复验 `hyacinth.iss` 兼容性。
