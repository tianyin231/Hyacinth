# 风信子需求讨论记录

> 本文件记录当前内部讨论形成的暂定方案，尚未获得客户最终确认。

## 1. 产品定位

- 软件定位为 Windows 本地 Excel 处理工作台。
- 核心流程为：导入文件、选择处理功能、生成新文件版本、通过版本演化树查看历史和分支。
- 历史版本不可被后续处理覆盖；用户可以从任意历史节点重新处理并产生新分支。

## 2. 首批 Excel 处理功能

暂定首批支持以下一进一出型功能：

1. 单列和多列排序。
2. 条件筛选。
3. 删除重复行。
4. 删除空白行。
5. 查找与替换。
6. 清除文本首尾空格。
7. 删除或调整指定列。
8. 自动调整列宽与表头样式。

分组汇总和类型转换暂定为第二阶段；版本差异对比已经确认纳入第一版。

文件拆分、文件合并和批量处理暂缓，因为它们涉及一对多或多对一关系，可能需要将版本树扩展为更复杂的版本关系图。

## 3. 轻量化单元格编辑

“Excel 文件预览”界面调整为轻量化 Excel 编辑区，暂定支持：

- 单元格选择和区域多选。
- 双击编辑文本、数字、日期和公式。
- 复制、剪切、粘贴和清空。
- 撤销和重做。
- 插入、删除行列。
- 修改字体、颜色、对齐方式和数字格式。
- 调整行高、列宽。
- 切换工作表。
- 保留原有公式和基础样式。

第一版暂不支持图表、图片、形状、宏、VBA、数据透视表编辑、完整公式计算引擎以及与 Excel 完全一致的高级填充操作。

## 4. 单元格编辑的保存方式

- 采用手动“保存为新版本”，不自动覆盖当前版本。
- 一次连续编辑视为一个编辑会话，保存时只生成一个“手动编辑”子节点。
- 存在未保存修改时，切换文件、切换版本节点或关闭软件，需要提示用户保存、放弃或取消。

## 5. 支持平台

- 第一版只支持 Windows 10 和 Windows 11。
- 使用 Windows 的文件选择器、桌面和文档目录、系统托盘及开机自启能力。

## 6. XLS 与 XLSX 处理方式

- 软件根据文件扩展名自动识别格式，不要求用户手动选择解析模式。
- 导入 `xlsx` 时，直接作为工作文件。
- 导入 `xls` 时，保留原始 `xls`，内部自动生成 `xlsx` 工作副本。
- 文件列表和根节点仍显示原始文件名及格式。
- 属性中显示原始格式和实际工作格式。
- 预览、单元格编辑和后续处理使用内部 `xlsx` 工作副本。
- 下载根节点时允许下载原始 `xls`；处理后的子节点统一为 `xlsx`。
- 内部格式转换不生成独立版本节点，版本树只记录用户执行的处理和手动编辑。
- 原需求中的 `xlwr` 应为 `xlwt`；采用转换方案后，不依赖 `xlwt` 写入后续工作版本。

## 7. 文件导入与内部存储

- 导入文件时，将原文件复制到软件管理的工作目录，不直接引用或修改用户原位置的文件。
- 用户移动或删除原位置文件后，软件内的文件及历史版本仍可正常使用。
- 软件内重命名只修改显示名称和内部副本，不影响用户原位置的文件。
- 每个导入文件使用内部唯一 ID 建立独立目录，避免同名文件和历史版本互相覆盖。
- 下载版本节点时，从内部文件库导出副本到用户设置的下载目录。
- 用户确认删除整个已上传文件后，先将内部文件及其完整版本树移入回收站；只有用户主动永久删除时才物理清除。

## 8. Excel 双引擎与降级策略

- 软件不能强制要求用户安装 Microsoft Excel。
- 软件启动或首次处理文件时检测 Microsoft Excel 是否可用。
- 已安装且可调用 Microsoft Excel 时，优先使用 Windows COM 引擎，提高 `xls` 转换、公式重算和复杂格式保存的兼容性。
- 未安装或无法调用 Microsoft Excel 时，自动使用 Python 引擎继续运行。
- Python 引擎使用 `openpyxl` 处理 `xlsx`，使用 `xlrd` 读取 `xls` 并转换为内部 `xlsx` 工作副本。
- 降级模式仍支持基础数据读取、单元格编辑和首批 Excel 处理功能，但复杂样式可能无法完整保留，公式可以保存但不能由 `openpyxl` 计算。
- 两种引擎使用相同的上层处理接口，版本树、文件管理和界面不需要感知底层引擎差异。
- 当前使用的引擎及兼容性限制应在文件属性或状态区域中显示。
- 进入 Python 兼容模式且当前操作可能影响复杂样式或公式结果时，只在首次风险操作前显示一次确认提示。
- 用户确认后不在每次操作时重复弹窗，但编辑区或状态栏需要持续显示“Python 兼容模式”标识。

## 9. 公式单元格显示与编辑

- 表格单元格默认显示公式计算结果，公式栏显示原始公式。
- 双击公式单元格时编辑原始公式，而不是计算结果。
- Excel 增强模式下，保存前调用 Microsoft Excel 重新计算公式。
- Python 兼容模式下，已有缓存结果标记为“未重新计算”。
- 用户修改公式或其引用数据后，无法由 Python 引擎计算的新结果标记为“待计算”，不能显示为最新结果。
- 文件之后使用 Microsoft Excel 打开时，允许 Excel 重新计算公式。

## 10. 工作表标签与管理

- 原需求中的 Excel“分页”按工作表 Sheet 标签理解，显示在编辑区底部。
- 第一版支持切换、新建、重命名、删除和拖动排序工作表。
- 工作表较多时，标签区域支持横向滚动。
- 删除工作表前需要用户确认，并且工作簿中至少保留一个可见工作表。
- 工作表管理操作归入当前编辑会话，用户点击“保存为新版本”后统一生成新节点。

## 11. Excel 编辑区右键菜单

- 第一版提供剪切、复制、粘贴、仅粘贴值、清空内容、插入行、删除行、插入列、删除列和设置单元格格式。
- 同时支持 `Ctrl+X`、`Ctrl+C`、`Ctrl+V` 和 `Delete` 等常用快捷键。
- 右键已选择的单元格区域时保留原选择范围，菜单操作作用于整个选择区域。
- 第一版暂不支持批注、超链接编辑和完整的选择性粘贴窗口。

## 12. 版本管理与删除回滚

- 版本管理参考 Git 的核心思想，但不直接使用 Git 存储 Excel 文件。
- 一个已导入文件及其全部版本相当于一个独立版本库。
- 每个版本节点保存完整且不可变的 Excel 快照，相当于一次提交。
- 节点记录父节点、生成时间、处理功能、处理参数、执行引擎和备注等信息。
- 选择历史节点相当于签出版本；后续编辑或处理以该节点为基础生成新分支，不修改原节点。
- 当前编辑会话相当于工作区；点击“保存为新版本”后才生成正式版本节点。
- 提供“恢复到此版本”操作，但恢复时不移动版本指针，也不覆盖或删除后续历史。
- 恢复操作复制目标历史节点的内容，并在当前节点之后生成一个新的“恢复版本”子节点。
- 第一版保留自动分支，不引入命名分支、变基、挑选提交和复杂合并。
- 后续可以增加 Excel 专用的单元格级版本差异对比和冲突处理。

删除和回滚规则：

- 删除节点时只标记为已删除，不修改数据库中的父子关系。
- 主版本树默认保留灰色“已删除版本”占位节点，使子节点连接关系和删除位置仍然清晰。
- 用户关闭“显示已删除节点”后，占位节点隐藏，其可见子节点临时显示为新根节点；这只是界面展示，不改变真实关系。
- 节点文件、父子关系和元数据仍然保留，恢复节点后原版本树自动恢复。
- 删除后提供即时“撤销”入口，同时可以在回收站中恢复。
- 删除整个已上传文件时，也将完整文件及版本树移入回收站。
- 回收站不自动过期，只有用户主动执行“永久删除”或“清空回收站”时才物理删除文件和记录。
- 永久删除必须再次确认，并明确提示无法恢复。

## 13. 版本差异对比

- 第一版支持选择两个版本节点进行 Excel 差异对比。
- 按工作表统计新增、删除和修改的单元格，并列出新增、删除和重命名的工作表。
- 新增、删除和修改内容分别使用不同颜色标识。
- 点击差异单元格时显示修改前、修改后的值和公式。
- 支持只显示存在差异的单元格。
- 第一版比较单元格内容和公式，格式差异暂不纳入比较。
- 第一版主要按照工作表名称和单元格坐标进行比较；插入整行或整列后可能产生较多位置差异。
- 后续可以增加由用户指定关键列的业务数据对比，识别行列移动后的同一条记录。
- 差异界面同时提供“单表高亮 + 修改详情”和“左右并排同步网格”两种查看方式。
- 用户首次进入差异界面时，默认使用“单表高亮 + 修改详情”，以保证列宽、性能和键盘操作体验。
- 用户可以随时切换到左右并排模式，用于查看结构变化和完整前后内容。
- 保存用户最后一次选择的差异查看方式；退出软件或重新进入版本对比时恢复该模式。
- 具体对比节点、滚动位置和当前差异项属于临时会话状态，不在下次打开时自动恢复。
- 左右并排模式支持同步滚动、同步缩放和同步 Sheet 切换，并允许用户临时解除同步。

## 14. 版本名称、备注与里程碑

- 处理功能自动生成版本名称和处理说明，手动编辑默认命名为“手动编辑”。
- 保存新版本时允许用户填写自定义版本名称和备注，但不强制填写。
- 版本节点记录来源版本、处理功能、处理参数、执行引擎和生成时间。
- 用户可以为重要版本添加或取消“里程碑”标记。
- 版本名称、备注和里程碑可以后续修改，但版本对应的 Excel 内容和父子关系不可修改。
- 内部使用唯一版本 ID；普通界面不展示类似 Git 提交哈希的复杂标识。

## 15. 未保存编辑的草稿恢复

- 编辑过程中自动保存临时草稿，但不自动生成正式版本节点。
- 正常切换文件、切换版本节点或退出时，提示用户保存、放弃或取消。
- 软件异常退出、断电或被强制关闭后，下次启动时提示用户恢复未保存编辑。
- 恢复草稿后仍处于未提交状态，由用户决定是否保存为新版本。
- 用户选择放弃草稿后，删除对应临时数据。
- 关闭到系统托盘不视为退出，继续保留当前编辑会话。
- 草稿只用于故障恢复，不显示在版本演化树中。

## 16. 功能参数面板与前端质量目标

- 选择 Excel 处理功能后，左上“功能选择”面板原位切换为参数表单，提供返回功能列表的入口。
- 参数配置期间保持版本树和 Excel 编辑区可见，不使用遮挡数据的居中弹窗，也不占用右侧设置抽屉。
- 参数表单至少包含处理工作表、处理区域、功能专属参数、执行引擎状态、版本名称、备注及执行操作。
- 正式前端定位为现代、专业、数据密集的 Windows 桌面生产力工具，低保真讨论线框不代表最终视觉质量。
- 视觉方向确定为更偏 Windows Fluent：使用 Segoe UI、系统式控件层级、局部 Mica/Acrylic 材质、柔和描边与阴影，并保持 Windows 10/11 的一致体验。
- Fluent 材质只用于标题栏、工具栏、侧栏、设置抽屉和弹层；Excel 网格保持不透明、清晰和高对比度。
- 已确认采用轻量 Fluent 方向：系统质感主要通过控件层级、状态反馈、柔和描边与局部材质表达，不使用整窗模糊或嵌套模糊。
- 视觉审查中确认提高辅助文字最小字号、降低多层阴影开销、补齐键盘焦点和禁用/按下状态，并支持系统“减少动画”设置。
- Excel 网格是视觉和交互主角；功能列表、文件列表和版本树保持紧凑清晰，不使用营销页面式大标题或大卡片布局。
- 浅色、深色和自定义主题统一使用语义化设计令牌；数据区保持清晰，壁纸和模糊效果不能降低单元格可读性。
- 图标使用统一的矢量图标体系，交互状态包含悬停、按下、选中、焦点、禁用和加载反馈。
- 常规交互反馈应在 100ms 内出现；微交互动效控制在 150-300ms，并支持系统减少动画设置。
- Excel 读取、保存、转换、公式重算和数据处理必须在后台执行，任何长任务都不能冻结界面。
- 大表格、文件列表和版本树采用按需渲染或虚拟化，滚动、缩放和面板拖动期间不得全量重建界面。
- 第一版验收基线暂定为 Windows 10/11、4 核 CPU、8GB 内存、1080p 屏幕；具体性能数据仍需在原型阶段实测校准。

## 17. 处理功能的预览与提交

- 所有 Excel 处理功能采用两阶段提交，不在执行后立即创建正式版本节点。
- 用户配置参数后点击“预览结果”，后台只执行一次处理并生成临时 Excel 草稿。
- 编辑区明确显示“临时结果，尚未生成版本”，用户检查后点击“应用并生成版本”。
- 确认时直接将临时结果升级为正式版本，不重复执行处理。
- 用户可以在临时结果上继续进行轻量化单元格编辑，再统一保存为新版本。
- 取消预览时删除临时结果，不在版本树中留下节点。
- 处理失败时不生成节点，需要显示错误原因和恢复建议。
- 耗时处理显示进度并允许取消，处理期间不能冻结主界面。
- 处理范围统一提供“当前选择区域”“当前连续数据区域”“当前工作表”和“全部工作表”。
- 执行功能前已选择多个单元格时，默认使用当前选择区域。
- 没有明确选择区域时，默认使用当前单元格所在的连续数据区域，避免误处理工作表远处的格式和备注。
- 用户可以在功能参数面板中手动切换处理范围。
- 软件自动识别处理区域的候选表头，但必须允许用户修正表头所在行或选择“该区域没有表头”。
- 自动识别可以参考文本占比、列名重复情况和下一行数据类型等特征。
- 参数面板明确显示当前识别的表头行，预览时固定并高亮该行。
- 没有表头时使用 `A、B、C...` 作为临时列名，不修改原文件内容。
- 处理全部工作表时，每个 Sheet 分别识别表头。

## 18. 第一版技术架构

- 第一版采用全 Python 架构，桌面前端使用 PySide6。
- Excel 编辑区使用 `QTableView + QAbstractTableModel + QStyledItemDelegate`，只渲染可见区域，不为每个单元格创建独立控件。
- 版本图谱使用 `QGraphicsView + QGraphicsScene`，节点位置与父子关系分离，缩放和平移使用视图变换。
- 主界面使用 Qt 原生布局、停靠和分隔能力实现面板拖动、换位、调整尺寸、隐藏与恢复。
- SQLite 使用 WAL 模式保存文件、版本节点、父子关系、处理参数、草稿、回收站、布局和设置等元数据。
- 每个正式版本保存独立且不可变的完整 Excel 快照，数据库只保存元数据和快照路径。
- Excel 读取、保存、转换、公式重算和处理功能在独立后台进程中执行，UI 进程只接收进度和结果，避免 Python 任务或 COM 阻塞界面。
- Fluent 外观使用自建设计令牌和 Qt 样式实现，不默认依赖可能具有 GPL 或商业授权要求的第三方 Fluent 组件库。
- Windows 11 可以使用系统 Mica 背景；Windows 10 降级为稳定的纯色 Fluent 表面，不依赖非公开模糊接口作为核心效果。
- 发布前需要确认 PySide6 LGPL 与最终打包方式的合规要求，优先使用可替换动态库的目录式安装包。
- 原型阶段必须使用大文件、大表格和多节点版本树进行真实性能测试，再校准正式验收上限。

版本文件存储策略：

- 正式版本优先保存完整快照，保证每个节点都能独立预览、下载和恢复。
- 保存前计算内容哈希；新内容与父版本完全相同时，不生成无意义版本。
- 同一个已上传文件记录内，内容完全相同的版本快照可以复用同一个物理文件，但保留各版本节点的名称、备注和关系，并对物理文件进行引用计数。
- 不同的已上传文件记录之间不复用物理文件，即使文件内容完全相同也保持各自独立的目录和副本。
- 文件属性中显示当前文件全部版本占用的磁盘空间，空间不足时在写入前提示。
- 不自动删除任何正式版本或回收站内容。
- 软件可以自动清理用户已取消的预览文件、明确放弃的草稿和其他临时缓存。

## 19. 首批处理功能规则

### 19.1 排序

- 排序功能始终保持完整数据行的字段对应关系，不提供只移动某一列单元格的危险模式。
- 用户选择的列只作为排序依据，实际移动当前数据区域内的整行记录。
- 支持单列和多列排序，每个排序列可以设置优先级、升序或降序。
- 表头不参与排序。
- 用户只选择单列时，需要明确提示将按该列排序完整数据行。
- 合并单元格、跨区域引用等可能破坏排序的数据，需要在预览前显示警告。

### 19.2 删除重复行

- 用户可以选择一个或多个关键列判断重复。
- 未选择关键列时，按照数据区域内整行所有列完全一致进行判断。
- 命中重复时删除整条数据行，不清空部分单元格。
- 默认保留第一次出现的记录，也可以切换为保留最后一次出现的记录。
- 提供是否忽略大小写和文本首尾空格的选项。
- 空值默认参与重复判断。
- 预览阶段显示重复组数、预计删除行数，并允许查看被删除行与保留行的对应关系。

### 19.3 删除空白行

- 默认只有当前数据区域内的所有单元格都为空，才判定为空白行。
- 只包含空格、制表符或换行符的文本可以视为空白。
- 包含公式的单元格即使显示结果为 `""`，默认也不视为空白。
- 用户可以选择“指定关键列全部为空时删除整行”。
- 只删除当前数据区域内的行，不处理区域外仅有样式的空行。
- 表头行永远不删除。
- 预览阶段显示预计删除的行号和数量。
- 合并单元格跨越目标行时停止处理并提示用户。

### 19.4 查找与替换

- 查找替换分为“值与文本”和“公式”两种模式，默认使用值与文本模式。
- 值与文本模式只修改普通单元格内容，不修改公式表达式。
- 公式模式查找并修改公式文本，启用时需要显示风险提示。
- 支持区分大小写、完全匹配整个单元格和忽略文本首尾空格。
- 支持当前区域、当前工作表和全部工作表三种范围。
- 支持只查找、逐项替换和全部替换。
- 预览阶段显示单元格坐标及修改前后的内容。
- 第一版不支持正则表达式，也不查找批注、超链接地址、工作表名称和单元格格式。

### 19.5 清除文本首尾空格

- 默认只处理文本单元格，不修改数字、日期和公式单元格。
- 删除文本开头和结尾的普通空格、制表符、换行符、全角空格和不间断空格。
- 默认保留文本中间的连续空格和换行。
- 可以额外开启“将中间连续空格压缩为一个空格”。
- 支持选择指定列，避免对姓名、地址和备注等敏感列进行不必要的全量处理。
- 预览阶段显示修改前后内容和受影响单元格数量。
- 清理后变为空字符串的单元格保持为空，不自动删除整行。

### 19.6 删除或调整行列

- Excel 增强模式下，由 Microsoft Excel 负责更新公式引用、命名区域等关联关系。
- Python 兼容模式下，结构修改前扫描受影响区域中的公式、合并单元格、表格、图表和命名区域。
- 未发现复杂引用时正常生成预览；发现潜在引用时显示影响清单和风险提示。
- 用户可以取消操作，或者明确选择继续生成兼容模式预览。
- 兼容模式结果需要标记“公式引用可能需要使用 Excel 检查”。
- 原版本始终保留，结构修改结果先进入临时预览，不直接生成正式节点。
- 宏文件和包含复杂外部链接的文件，第一版禁止执行结构性行列修改。

### 19.7 自动列宽与表头样式

- 默认只处理识别到的数据区域。
- 自动列宽根据表头和实际内容计算，并设置最小、最大列宽。
- 超过最大宽度的文本启用换行，不继续扩大列宽。
- 默认只修改表头行和列宽，不覆盖数据区域已有的字体、颜色、边框和数字格式。
- 表头使用与当前主题协调的浅色底、加粗文字和细边框。
- 可以选择冻结表头行、启用筛选按钮和调整行高。
- 用户可以分别关闭“调整列宽”和“设置表头样式”。
- 大文件采用内容抽样计算列宽，避免为样式处理扫描全部数据。
- 预览阶段列出即将改变的格式项目。

### 19.8 条件筛选

- 默认采用“应用筛选”，不删除数据，只隐藏不匹配的行，并保留 Excel 筛选条件。
- 可以选择“筛选并提取”，将匹配行复制到一个新的工作表，不修改原工作表。
- 第一版不提供直接物理删除未匹配行的筛选模式。
- 支持等于、不等于、包含、不包含、大于、小于、介于、为空和不为空。
- 支持多个条件使用“并且”或“或者”组合。
- 日期、数字和文本分别使用对应的数据类型进行比较。
- 预览阶段显示匹配数量、总数量和匹配比例。

## 20. 存储目录迁移

- “已上传文件目录”包含原始导入文件、内部工作副本、正式版本快照、草稿、临时预览、回收站和 SQLite 数据库。
- 用户修改目录时迁移整个文件库，不允许只让新文件写入新目录而将版本库分散到多个位置。
- 迁移前检查新目录的可写权限和可用空间，并暂停新的处理与保存任务。
- 先复制完整文件库并校验文件哈希，校验成功后再切换当前数据库和工作目录。
- 迁移失败时继续使用旧目录，不留下半迁移状态；新目录中的未完成副本作为临时数据清理。
- 成功切换后才清理旧目录，清理前向用户确认。
- 第一版只允许迁移到本地固定磁盘，不支持网络盘和移动设备。

## 21. 版本导出

- 右键“下载该节点”时直接导出到默认下载目录，同时提供“另存为”操作。
- 默认文件名格式为“原文件名_版本名称_生成时间.扩展名”。
- 文件名中的非法字符自动替换；版本名称过长时截断，但保留生成时间和扩展名。
- 目标目录存在同名文件时自动追加 `(1)`、`(2)`，绝不静默覆盖已有文件。
- 下载完成后提供“打开文件”和“打开所在位置”。
- 下载根节点时保留原始格式；处理后的版本节点统一导出为 `xlsx`。

## 22. 重复导入

- 使用内容哈希识别完全相同的文件，但重复不构成禁止导入条件。
- 文件内容完全相同时，提示用户打开现有记录或“作为独立文件导入”。
- 用户选择独立导入后，创建新的文件 ID、根版本、内部目录和原始文件物理副本。
- 独立记录的版本树、草稿、回收站和删除行为互不影响，不考虑重复文件造成的额外空间占用。
- 文件名相同但内容不同时，直接作为独立文件导入。
- 文件列表允许同名记录，第二个同名显示名称默认增加 `(2)`，用户可以之后重命名。
- 第一版不提供“将再次导入的文件追加为现有文件新根节点”。

## 23. 桌面壁纸作用范围

- 桌面壁纸默认覆盖整个软件，包括功能区、文件列表、版本树和 Excel 编辑区。
- 设置中提供“壁纸不进入 Excel 网格”选项；启用后壁纸只显示在窗口背景和其他面板中，Excel 网格使用纯色不透明背景。
- 单元格已有的填充颜色、字体颜色和其他工作簿样式必须保持原样，壁纸只影响界面渲染，不写入 Excel 文件。
- 全覆盖模式下，默认无填充的单元格区域使用半透明主题遮罩，使壁纸能够透出，同时保证文字和网格线可读。
- 提供“网格壁纸可见度”滑块，允许用户在完整范围内自由调整，不设置 30% 的硬上限。
- 滑块在 30% 位置显示建议阈值，建议范围使用低调的安全区标识，但不阻止用户继续提高可见度。
- 用户开始拖动滑块时，设置抽屉暂时隐藏，只保留紧凑的浮动滑块和值提示，主界面实时显示最终效果。
- 拖动过程中保持鼠标捕获并按帧更新预览；松开鼠标后保存数值并恢复设置抽屉，按 `Esc` 恢复拖动前的数值。
- 壁纸模糊度滑块采用相同的实时预览交互。
- 编辑区未打开文件时，空状态可以直接显示壁纸。
- 模糊处理基于缓存后的壁纸生成，只在更换图片、调整模糊度或窗口尺寸稳定后重新计算，不在滚动和拖动时实时重复模糊。

## 24. 自定义强调色

- 原需求中的“用户自定义主题”调整为“强调色”。
- 保留红、橙、黄、绿、青、蓝、紫七个推荐色板，使用圆形颜色样本展示。
- 增加 Windows 系统颜色选择器，允许用户选择任意强调色。
- 用户选择色板或自定义颜色后立即预览并保存，点击弹层外部时关闭颜色选择弹层。
- 强调色只用于主要按钮、选中状态、焦点环、当前版本节点和其他交互强调。
- 错误、警告、成功、删除和差异对比等语义色保持独立，不跟随强调色变化。
- 用户选择基础强调色后，软件自动生成悬停、按下、选中、焦点和禁用状态的颜色变体。
- 根据背景自动选择黑色或白色前景文字，普通文字对比度至少达到 `4.5:1`。
- 颜色对比不足时保留用户选择的原始颜色样本，但实际控件使用自动校正后的可访问变体。
- 颜色选择器显示对比度状态，并允许查看原始颜色和实际应用颜色。

## 25. 设置抽屉

- 设置界面使用右侧覆盖式抽屉，从窗口右边缘向左滑入，关闭时向右滑出。
- 抽屉覆盖 Excel 编辑区右侧，不重新调整四个主面板宽度，避免网格和版本树发生布局跳动。
- 默认宽度约为 `380px`，小窗口下根据可用宽度自适应。
- 打开和关闭动画约为 `220ms`，并遵守系统“减少动画”设置。
- 点击抽屉外区域或按 `Esc` 可以关闭。
- 存在未确认的目录迁移等重要操作时，关闭前提示用户。
- 调整壁纸可见度或模糊度时，暂时隐藏抽屉，仅保留紧凑滑块和值提示。

## 26. 主界面区域交换与边界拖动

- “功能选择”“已上传文件”“版本演化树”和“Excel 编辑区”是四个可交换位置的界面区域。
- 用户按住区域标题栏拖到另一个区域的位置后，两个区域交换槽位；第一版不允许区域脱离主窗口成为独立浮动窗口。
- 拖动标题栏时显示半透明预览和可放置区域，按 `Esc` 可以取消。
- 拖动过程只移动预览，不实时重建 Excel 网格或版本树。
- 所有界面区域之间的共享分隔线都可以拖动，竖向分隔线调整相邻区域宽度，横向分隔线调整上下区域高度。
- 主窗口外边缘和四角也可以使用 Windows 标准方式调整整个窗口大小。
- 区域交换后，进入新槽位的界面需要根据槽位宽高自适应，分隔线位置与区域内容相互独立。
- 软件退出时保存区域位置、分隔比例、隐藏状态和窗口尺寸。
- 右键标题栏提供移动区域的菜单，作为鼠标拖动以外的操作方式。
- 分隔线可以拖过完整可用范围；相邻区域缩小到约 `48px` 以下时显示“松开后隐藏”的视觉提示。
- 只有用户松开鼠标后才真正隐藏区域，拖动过程中可以拉回，避免快速拖动造成误隐藏。
- 隐藏后保留可交互的边缘分隔线，右键可以恢复隐藏区域，双击可以恢复推荐尺寸。

## 27. 功能与文件搜索

- 功能列表和文件列表采用统一搜索交互，点击搜索图标后标题栏原位切换为搜索输入框。
- 输入时使用约 `150ms` 延迟实时过滤当前列表，并高亮匹配文字。
- 功能搜索匹配功能名称和关键词；文件搜索匹配显示名称、扩展名、备注和导入时间。
- 搜索只过滤列表，不改变当前选中的文件、版本节点和 Excel 编辑内容。
- 按 `Esc`、点击关闭按钮或再次点击搜索图标时，清空搜索并恢复原标题。
- 没有结果时显示空状态，不隐藏其他界面区域。
- 第一版支持中文、英文和数字的普通包含匹配，不支持拼音首字母搜索。

## 28. 文件列表顺序

- 文件列表采用手动排序模式，新导入文件始终插入第一行并自动选中。
- 新文件插入后，其他文件保持原有相对顺序。
- 用户拖动文件后保存新的手动顺序。
- 搜索过滤期间禁止拖动排序，退出搜索后恢复。
- 重命名、打开和生成新版本不会改变文件顺序。
- 从回收站恢复文件时优先恢复到删除前的位置；原位置无法恢复时放到顶部。
- 第一版不增加按名称、时间或大小自动排序，避免与手动顺序产生冲突。

## 29. 版本预览与当前工作版本

- 版本树区分“预览节点”和“当前工作版本”，当前工作版本相当于 Git 的 `HEAD`。
- 单击历史节点只在 Excel 编辑区预览该版本，不立即改变当前工作版本。
- 用户点击“从此版本继续”或双击节点后，才将该节点设为当前工作版本。
- 从历史节点继续时，明确提示后续保存会创建新分支。
- 新处理或手动编辑版本提交成功后，新节点自动成为当前工作版本。
- 预览节点使用普通选中状态，当前工作版本使用明确的 `HEAD` 标记。
- 用户直接尝试编辑预览节点时，先询问是否从该版本创建新分支。

## 30. 删除当前工作版本

- 删除普通预览节点时不改变当前工作版本 `HEAD`。
- 删除 `HEAD` 前必须先处理未保存草稿和临时预览结果。
- 确认删除后，`HEAD` 自动切换到最近的未删除父节点。
- 没有可用父节点但存在子节点时，要求用户选择一个子节点作为新的 `HEAD`。
- 文件只有一个根节点时，不允许单独删除该节点；需要从文件列表删除整个文件记录并移入回收站。
- 恢复被删除的原 `HEAD` 后，不自动改变当前工作状态，用户可以手动切换。
- 删除确认界面明确显示删除后 `HEAD` 将切换到哪个节点。

## 31. 撤销、重做与版本恢复

- `Ctrl+Z` 和 `Ctrl+Y` 只作用于当前尚未保存的编辑会话。
- 单元格编辑、复制粘贴、清空、格式修改、结构性行列操作和工作表管理都进入当前撤销栈。
- 正式保存后的版本节点保持不可变，不能使用普通撤销修改其内容。
- 保存为新版本成功后清空当前撤销栈。
- 需要撤销已保存内容时，使用“恢复到历史版本”，并生成新的恢复节点。
- 切换当前工作版本前提示保存或放弃，确认切换后清空旧会话的撤销栈。
- 批处理结果仍处于临时预览时，可以直接取消整个预览。

## 32. 工作区状态恢复

- 保存主窗口位置、尺寸和最大化状态。
- 保存四个界面区域的位置、尺寸比例和隐藏状态。
- 保存当前选中的文件、当前工作版本 `HEAD`、当前预览节点和当前工作表。
- 保存 Excel 网格的缩放比例与滚动位置。
- 保存版本树的缩放比例、平移位置和节点显示位置。
- 不保存搜索栏打开状态和搜索关键词，启动时保持搜索关闭。
- 设置抽屉关闭后，启动时不自动重新打开。
- 未保存编辑通过草稿恢复提示处理。
- 正在执行的任务不自动重启，只记录并提示“上次任务异常中断”。
- 上次文件或节点已经进入回收站时，启动后选择最近可用的文件和版本。

## 33. 后台任务队列

- 第一版采用全局单任务队列，同一时间只执行一个 Excel 读取、转换、处理或保存任务。
- 后续任务进入队列等待，不同时占用大量内存。
- 任务执行期间 UI 保持可交互，用户可以查看其他文件和历史版本。
- 当前任务显示名称、所属文件、进度、耗时和实际执行引擎。
- 用户可以取消当前任务，也可以从队列中移除尚未开始的任务。
- Excel COM 任务与 Python 任务共用同一个队列，避免两个引擎同时操作工作文件。
- 临时预览、正式版本和数据库记录只在任务成功完成后创建。
- 第一版不支持多个 Excel 任务并行执行，后续根据真实性能测试决定是否扩展。

## 34. 运行任务时关闭软件

- 开启“关闭到托盘”后，点击关闭按钮只隐藏窗口，后台任务继续执行，完成后发送系统通知。
- 未开启“关闭到托盘”且没有运行任务时，正常退出软件。
- 未开启“关闭到托盘”但任务正在运行时，提供“等待完成后退出”“取消任务并退出”和“返回软件”。
- 取消任务并退出时，先请求后台任务安全停止并清理临时文件，再关闭进程。
- Excel COM 正在执行不可立即中断的保存操作时，显示安全收尾状态，不强制终止正在写入的文件。
- Windows 关机时停止接受新任务，尽力取消当前任务并清理临时文件。

## 35. 数据库备份与版本树重建

- 软件启动时执行 SQLite 完整性检查。
- 数据库结构升级、文件库迁移和批量删除前自动备份数据库。
- 每天首次启动时生成一份数据库备份，保留最近 7 份。
- 每个版本快照旁保存最小恢复清单，记录版本 ID、父节点、文件哈希、创建时间和处理信息。
- SQLite 损坏时可以根据快照文件和恢复清单重建版本树。
- 所有正式写入先写入临时文件，校验成功后再原子替换目标文件。
- 恢复过程只读取现有文件，不覆盖版本快照；重建结果由用户确认后再启用。

## 36. 第一版文件格式

- 第一版只支持导入 `.xlsx` 和 `.xls`。
- `.xlsx` 直接作为工作文件，`.xls` 保留原文件并转换为内部 `.xlsx` 工作副本。
- 第一版不支持 `.xlsm`、`.csv`、`.xlsb` 和 `.ods`。
- 文件选择器只显示支持的格式。
- 用户拖入不支持的文件时，明确说明不支持原因，不尝试强行解析。
- CSV 导入可以作为后续独立功能评估，不混入第一版核心范围。

## 37. 开机自启与系统托盘

- 开启开机自启后，Windows 登录时软件静默启动到系统托盘，不自动弹出主窗口。
- 开机自启阶段不自动打开上次文件，也不自动恢复未保存草稿，直到用户主动打开窗口。
- 检测到上次异常退出或数据库问题时，托盘图标显示提示，用户打开主窗口后处理。
- 托盘菜单提供“打开风信子”“当前任务”“暂停接收新任务”和“退出”。
- 用户手动启动软件时正常显示主窗口。
- “开机自启”和“关闭到托盘”保持为两个独立设置。

## 38. 版本图谱排布

- 新导入的根节点默认放在左侧，子节点默认放在父节点右侧。
- 同一父节点的多个分支纵向展开，自动避免节点重叠。
- 用户手动拖动节点后，该节点位置被固定；新增其他节点时不移动已经固定的节点。
- “自动整理”可以重新计算全部节点位置，执行前保留旧布局并允许立即撤销。
- 拖动节点只改变显示位置，不改变父子关系。
- 提供“适应窗口”“恢复 100%”和缩放控件。
- 节点位置、固定状态、缩放比例和平移位置随工作区保存。

## 39. 文件重命名

- 重命名只修改软件内的显示名称，不修改用户最初导入位置的原文件。
- 不修改内部 UUID、版本快照路径和版本关系。
- 扩展名不可直接编辑，避免将改名误认为格式转换。
- 根节点和页眉同步显示新的名称，文件属性中始终保留原始文件名。
- 后续导出默认使用当前显示名称。
- 允许不同文件记录使用相同显示名称，但显示重名提示。
- `M` 快捷键继续用于重命名，输入框只选中文件名主体，不选择扩展名。

## 40. 文件属性

- 普通属性显示当前显示名称、原始文件名、原始格式、内部工作格式、导入时间、最后打开时间和原始文件大小。
- 显示工作表数量与名称、数据区域行列规模和公式单元格数量。
- 显示当前工作版本、版本数量、分支数量、里程碑数量和全部版本磁盘占用。
- 显示当前使用的 Excel 引擎和用户备注，普通属性只读，用户备注可以编辑。
- 文件 ID、内容哈希、内部存储路径、原始导入路径和数据库记录状态放入折叠的“高级信息”。
- 统计信息按需在后台计算，打开属性界面时不能冻结软件。

## 41. 版本节点右键菜单

- 版本节点菜单提供预览、从此版本继续、与当前版本对比、设为对比基准、恢复到此版本、下载、另存为、编辑名称与备注、里程碑、版本详情和删除到回收站。
- 当前 `HEAD` 节点明确显示标记。
- 恢复历史版本时生成新的恢复节点，不覆盖历史。
- 危险操作与普通操作使用分隔线隔开，删除放在菜单最底部。
- 已删除占位节点的菜单只提供查看详情、恢复和永久删除。
- 不可用操作显示禁用状态并说明原因，不直接隐藏。

## 42. 快捷键

- `Ctrl+O` 导入文件，`Ctrl+S` 保存为新版本。
- `Ctrl+Z` 和 `Ctrl+Y` 撤销、重做当前未保存编辑。
- `Ctrl+C`、`Ctrl+X` 和 `Ctrl+V` 复制、剪切、粘贴。
- `Ctrl+F` 搜索当前获得焦点的界面区域。
- `F2` 重命名选中的文件或版本，`Alt+Enter` 查看属性或版本详情。
- `Delete` 删除当前获得焦点的对象，仍然执行对应确认或软删除规则。
- `Esc` 取消编辑、拖动、弹层或临时预览。
- `Ctrl+鼠标滚轮` 缩放当前获得焦点的 Excel 网格或版本图谱。
- 客户原文提出的 `M/R/D` 只作为右键菜单打开后的菜单访问键，不作为全局快捷键，避免输入文字时误触删除。

## 43. 待与客户复核

1. 首批 Excel 处理功能及其具体参数。
2. 轻量化单元格编辑的功能边界。
3. 手动编辑是否必须保存为新版本。
4. 第一版是否仅支持 Windows 10/11。
5. `xls` 自动转换为 `xlsx` 的兼容方案及下载规则。
6. 是否删除原需求中的 `xls/xlsx` 手动切换开关。
7. Excel“分页”是否指工作表 Sheet 标签。
8. Excel 编辑区右键菜单需要包含哪些操作。
9. 删除中间版本节点后，子节点成为新根节点的具体展示方式。
10. Excel 双引擎的自动选择方式及降级提示。
11. Git 风格版本管理的第一版功能范围。
12. 第一版版本差异对比的范围和坐标对比限制。
13. 版本名称、备注和里程碑标记的交互方式。
14. 未保存编辑的草稿恢复机制。
15. 左侧就地功能参数面板及其操作流程。
16. 前端视觉方向和流畅度验收指标。
17. Excel 处理功能的预览与两阶段提交规则。
18. PySide6 全 Python 架构、打包方式和许可证合规。
19. 首批处理功能的具体参数与数据安全规则。
20. 完整文件库迁移规则及第一版目录限制。
21. 版本文件的直接下载、另存为和命名规则。
22. 相同文件的独立导入及跨记录不共享物理文件规则。
23. 壁纸默认全覆盖及可关闭 Excel 网格壁纸的规则。
24. 七个推荐强调色和任意颜色选择器。
25. 右侧覆盖式设置抽屉及动画规则。
26. 四个界面区域的槽位交换、边界拖动和第一版不支持浮动窗口。
27. 功能列表与文件列表的统一搜索行为。
28. 文件列表中新文件置顶和用户手动排序规则。
29. 版本预览节点与当前工作版本 `HEAD` 的分离规则。
30. 删除当前工作版本后的 `HEAD` 切换规则。
31. 未保存内容使用撤销、已保存内容使用版本恢复的规则。
32. 软件重启后的工作区状态恢复范围。
33. 第一版全局单后台任务队列。
34. 后台任务执行期间关闭软件的处理规则。
35. SQLite 自动备份、完整性检查和版本树重建能力。
36. 第一版只支持 `xls` 和 `xlsx` 的格式边界。
37. 开机自启时静默进入系统托盘的规则。
38. 版本图谱自动排布、手动固定和自动整理规则。
39. 软件内文件显示名称的重命名规则。
40. 文件属性界面显示内容和高级信息范围。
41. 扩展后的版本节点右键菜单。
42. Windows 常用快捷键和右键菜单访问键规则。
