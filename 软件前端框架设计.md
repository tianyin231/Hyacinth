========================================================================
以下提到的“功能选择”界面、“已上传文件”界面、“版本演化树”界面、“Excel文件预览”界面
它们的“标题栏”都可以被用户使用鼠标左键长按从而实现自由拖动，
从而实现整个界面被用户自由拖动，
当用户将被拖动的界面拖动到其他界面的位置时，两个界面的位置将会互换，
用于实现该界面和其他界面互相交换位置。

界面中存在的滚动条，可以使用鼠标左键长按后实现自由拖动，
用户可以使用鼠标滚轮实现滚动条的上下滚动，
按住Shift时，使用鼠标滚轮实现下方滚动条的左右滚动。

其中在“版本演化树”界面和“Excel文件预览”界面中，当用户选中该界面后，
按住Ctrl时，使用鼠标滚轮实现缩放的效果。

软件关闭后，将保留用户退出时的软件状态。
=======================================================================

========================================================================
“功能选择”界面：

鼠标左键单击“🔍”后，“功能选择界面”标题变为“搜索导航栏”，可以在“搜索导航栏”输入关键词查找相对应的“软件功能”，
鼠标左键再次单击后“🔍”，“搜索导航栏”重新变成变成“功能选择界面”标题。

“功能选择”标题下方是可供选择的“软件功能”界面，点击其中一个“软件功能”后，
该“软件功能”会显示为被选中的状态。

“功能选择”界面的右侧是“滚动条”，可以上下滑动。

“功能选择”界面和“已上传文件”界面互为一体，它们的右侧边界可以使用鼠标左键长按拖动，用于调整两个界面的左右宽度，
当滑动的幅度过大可导致“功能选择”界面、“已上传文件”界面、“版本演化树”界面被隐藏，
并且界面被隐藏时， 用户可以使用鼠标右键单击它们之间的交界边界，会出现一个“上下文菜单”，
“菜单项”是：“取消隐藏”，
鼠标左键单击“取消隐藏”后，被隐藏的“功能选择”界面、“已上传文件”界面、“版本演化树”界面将重新出现。
如果没有界面被隐藏，用户可以使用鼠标右键单击界面的交界边界，也会出现一个“上下文菜单”，
“菜单项”是：“隐藏左侧界面”和“隐藏右侧界面”。
鼠标左键单击“隐藏左侧界面”后，“功能选择界面”、“已上传文件”界面将会被隐藏，
鼠标左键单击“隐藏右侧界面”后，“版本演化树”界面将会被隐藏，

“功能选择”界面和“已上传文件”界面的交界边界可以使用鼠标左键长按拖动，
用于调整两个界面的长度，界面会进行自适应。
当滑动的幅度过大可导致“功能选择”界面或“已上传文件”界面被隐藏，
并且界面被隐藏时， 用户可以使用鼠标右键单击后，会出现一个“上下文菜单”，
“菜单项”是：“取消隐藏”，
鼠标左键单击“取消隐藏”后，被隐藏的“功能选择”界面或被隐藏的“已上传文件”界面将重新出现。
如果没有界面被隐藏，用户使用鼠标右键单击界面的交界边界，也会出现两个“提示按钮”：“鼠标左键单击隐藏上侧界面”和“鼠标左键单击隐藏下侧界面”。
鼠标左键单击“鼠标左键单击隐藏上侧界面”后，
“功能选择界面”将会被隐藏，
鼠标右键单击“鼠标右键单击隐藏下侧界面”后，
“已上传文件”界面将会被隐藏。
========================================================================

========================================================================
“已上传文件”界面
鼠标左键单击“🔍”后，“已上传文件”标题变为“搜索导航栏”，可以输入关键词查找相对应的“已上传文件”，
鼠标左键再次单击“🔍”后，“搜索导航栏”重新变成“已上传文件”标题。

“已上传文件”标题下方是所有已上传到软件中的“文件”，
它们的格式是“已上传文件名.后缀”，
鼠标左键单击“已上传文件”界面下方的“已上传文件名.后缀”后，
该“已上传文件”会显示为被选中的状态，
且“版本演化树”界面将会显示该“已上传文件名.后缀”的版本演化树，
“Excel文件预览”界面将会显示该“已上传文件名.后缀”的预览。

鼠标右键点击“已上传文件”界面下方的“已上传文件名.后缀”后，
它的旁边将会出现一个“右键菜单”，其中有三个选项，
分别是“重命名”、“属性”、“删除”，
“重命名”可以使用快捷键M来完成，之后该文件的“旧的文件名”将会变为“全选状态”，
用户可以输入“新的文件名”来替换掉“旧的文件名”。

“属性”可以使用快捷键R来完成，之后将会显示该文件的属性信息。
“删除”可以使用快捷键D来完成，之后将会出现一个“弹窗”，
并且询问用户是否删除该文件，弹窗下方有两个按钮“确认”、“取消”，
当用户点击弹窗下方的“确认”按钮后，在“已上传文件”界面删除该文件，
当用户点击弹窗下方的“取消”按钮后，“弹窗”消失，无事发生。

界面右侧是“滚动条”，可以上下滑动。

鼠标左键可以通过长按“已上传文件”来完成上下拖动该文件的动作，并且能根据这个方法更换文件的上下顺序。

用户将文件上传到软件上之后，软件的默认文件存储地址是电脑本地的“文档”，
用户可以通过电脑本地的“文档”来找到自己上传的文件的备份。
========================================================================

========================================================================
“版本演化树”界面
用户导入文件后，该文件将会以“已上传文件名.后缀”在“已上传文件”界面第一行显示，
会导致原本第一行的“已上传文件名.后缀”变成第二行，
且最新上传的“已上传文件名.后缀”会显示为被选中的状态，
在“版本演化树”界面中显示该文件专属的“版本演化树”。

用户可以使用鼠标左键单击、长按一次“版本演化树”界面中的任何内容，
使其进入被选中状态，

“版本演化树”界面中，
使用“树形结构”来显示该文件的历史“根节点、父节点、子节点”，
（类似“物联网中间件”等“数据处理流水线”的“版本演化树”，使用树形结构）
“根节点”是用户导入文件后，它在电脑本地“文档”或其“子文件”中的文件，
其下方必定显示该文件的“已上传文件名.后缀”和其“导入时间”。

完成一次处理后，“父节点”将会在其后生成一个“子节点”。
“父节点”下一个“子节点”中显示
它是“父节点”使用了什么“软件功能”后产生的，并且显示“处理时间”。

用户通过使用鼠标左键单击某个历史“父节点”后，
“Excel文件预览”界面将会显示该节点对应的文件的内容，
来查看它所对应的excel文件。
但是不会导致该“父节点”后的“子节点”被更改或者被删除、覆盖。
历史“父节点”对应的文件，也能再次使用“软件功能”进行处理，
完成处理后生成新的“子节点”，
但是依然不会导致该“父节点”之后的“子节点”被更改或者被删除、覆盖。
“父节点”与其对应的“子节点”使用箭头相互连接，
并且箭头由“父节点”指向“子节点”。

用户可以使用鼠标左键长按“某一节点后”，
自由拖动“版本演化树”中的任意一个节点到“版本演化树”界面中的任意位置，
但是不会导致“父节点”和“子节点”之间的连接断裂，
也不会导致“父节点”和“子节点”之间的从属地位发生交换。

“版本演化树”界面的右侧和下方是“滚动条”，可以上下滑动。

“版本演化树”界面和“Excel文件预览”界面的交界边界，可以使用鼠标左键长按拖动，
界面会进行自适应。
用于调整两个界面的左右宽度，左右拖动的幅度没有限制，
如果导致“版本演化树”界面或者“Excel文件预览”界面被隐藏，
用户可以使用鼠标右键单击交界边界，会出现一个“上下文菜单”，
“菜单项”是：“鼠标左键单击取消隐藏”，
鼠标左键单击“菜单项”后，被隐藏的“功能选择”界面或被隐藏的“Excel文件预览”界面将重新出现。
如果没有界面被隐藏，用户可以使用鼠标右键单击交界边界，也会出现一个“上下文菜单”，
“菜单项”是：“鼠标左键单击隐藏左侧界面”和“鼠标左键单击隐藏右侧界面”。
鼠标左键单击“鼠标左键单击隐藏左侧界面”后，
“版本演化树”界面将会被隐藏，
鼠标右键单击“鼠标右键单击隐藏右侧界面”后，
“excel文件预览”界面将会被隐藏。

如果用户需要下载目标节点的Excel文件，
用户使用鼠标右键单击目标节点，将会弹出一个“上下文菜单”，
“菜单项”是：“下载该节点”和“删除该节点”，
鼠标左键单击“下载该节点”后，该节点的Excel文件将会被下载到默认下载地址。
鼠标左键单击“删除该节点”后，该节点和其对应的Excel文件将会被删除，
如果该节点为“父节点”且其存在“子节点”，则不删除其“子节点”，
此后该失去“父节点”的“子节点”将成为新的“根节点”独立存在，
意味着该“已上传文件.后缀”文件在“版本演化树”中存在两个独立的“根节点”，
新的“根节点”将会和最初的“跟节点”一样，标注“已上传文件名.后缀”和其“生成时间”。
========================================================================

========================================================================
“Excel文件预览图”界面
当“已上传文件”界面中的“已上传文件名.后缀”为选中状态时，
该界面将会显示该“已上传文件名.后缀”的预览。

“Excel文件预览图”界面右侧为垂直滚动条，下方的右侧为水平滚动条。

“Excel文件预览图”界面下方的左侧为Excel文件的“分页”，用户可以点击不同的“分页”实现预览该excel文件的其他分页的效果。

当用户右键该界面后，将弹出一个“上下文菜单”，
========================================================================

========================================================================
页眉
页眉左侧写有软件的名字“风信子”和“已上传文件”界面中被选中的文件“已上传文件.后缀”。
举例：风信子——水果店铺销售报表.xlsx

页眉右侧从左到右，分别是“设置”“最小化”“最大化”“关闭”图标，
鼠标指针悬停在其上时，会显示它们的名称，如“关闭”。

用户点击“设置”后进入设置界面，其他“最小化”“最大化”“关闭”等功能和一般网页的功能一样。
========================================================================

========================================================================
工具栏
按钮“+导入新文件”，
鼠标左键单击“+导入新文件”按钮后，
进入系统默认的“文件选择界面”且默认打开的地址是该电脑操作系统的“桌面”地址，
目的是便于用户选择自己需要的文件。
========================================================================

========================================================================
单击“设置”界面后，将会在“Excel文件预览”界面右侧出现一个“应用设置”界面，
“设置”界面启用时，不是突然出现，关闭时，也不是突然消失，
而是启用设置时，设置一个从左往右弹出的动画，关闭设置时，设置一个从右往左弹入的动画。

“设置”界面从上到下，第一个功能是“目录”
“已上传文件”标题下方，用户可以自定义“已上传文件”的地址，
用户导入新文件后，将会在已设置的目录地址或其子文件中出现。
“版本演化树”中生成的文件也保存在“已上传文件”的目录地址或其子文件中，
默认的地址是电脑的“文档”。
“导出文件地址”标题下方，用户可以自定义“文件默认下载地址”，用户下载文件后，
默认将文件下载到电脑的“桌面”。
“桌面壁纸上传地址”标题下方，用户可以自定义“桌面壁纸”选择的地址，
默认地址是电脑的“桌面”。

“设置”界面从上到下，第二个功能是“主题”，主题的种类有“浅色”、“深色”、“跟随系统”、“用户自定义”、“桌面壁纸”。
软件的默认主题是“浅色”，“浅色”、“深色”的主题样式由你自由发挥，
“浅色”、“深色”两个主题实际上是白色主题和黑色主题，
“跟随系统”需要软件拥有获取该电脑系统使用的“主题”的能力。

“浅色”按钮、“深色”按钮、“跟随系统”按钮，并列一行，
“用户自定义”和“桌面壁纸”按钮与以上三种选项按钮分开，新设置一行，
在这行的左侧是“用户自定义”的按钮，右侧是“桌面壁纸”按钮，
当用户点击“用户自定义”的按钮后，将在“用户自定义”下方弹出一个窗口，
窗口中有7中圆圈预览的主题配色，分别是红橙黄绿青蓝紫，
用户选择其中一个色号后，主题颜色替换成用户选择的色号，并且自动关闭弹窗，
用户的鼠标左键单击弹窗以外的地方，也会自动关闭弹窗。
当用户点击“桌面壁纸”的按钮后，将会出现一个弹窗，
弹窗中有一个“选择图片”按钮，一个“恢复默认”按钮，一个“滑块”，
用户点击“选择图片”按钮后，打开一个操作系统默认的“资源管理器”界面，
默认地址是当前电脑的桌面，用户选择一张图片作为壁纸后，
这张壁纸将会显示在“功能选择”界面、“已上传文件”界面、“版本演化树”界面、“Excel文件预览”界面上，
不影响其他区域和软件“主题”，
用户点击“恢复默认”按钮后，“桌面壁纸”将会恢复为软件默认壁纸，
用户选择“选择图片”并设置为“桌面壁纸”后，可以拖动滑块，来修改壁纸图片的模糊度。

“设置”界面从上到下，第三个功能是“滑动框组”。
其中有两个内容，“开机自启”和“关闭到托盘”，
“开机自启”的“滑动框组”默认为关闭状态，开启后，
电脑开机后会自动启动该软件，否则电脑开机后不会自动启动该软件。
“关闭到托盘”的“滑动框组”默认也为关闭状态，开启后，
用户点击“关闭”图标后，软件依然在后台，可以在“托盘”中找到并且关闭它，
否则，点击“关闭”图标后，软件直接关闭。
========================================================================


========================================================================
“xls和xlsx”切换选项卡（开关、按钮）。
xls文件需要使用Python库“xlrd”完成“读”操作，
xlsx文件需要使用Python库“xlwr”完成“写”操作。
xlsx文件需要使用openpyxl库完成“读写”操作。
默认该“切换选项卡“为xlsx状态，鼠标左键点击后前换成xls状态，
鼠标左键再次点击后恢复为xlsx状态。
xlsx状态用于处理xlsx文件，使用openpyxl库完成“软件功能”操作，
xls状态用于处理xls文件，使用xlrd和xlwr库完成“软件功能”操作。
========================================================================

























